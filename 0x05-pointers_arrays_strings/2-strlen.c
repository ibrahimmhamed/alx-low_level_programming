#include "main.h"
#include <string.h>

/**
 * _strlen - returns the length of a string.
 *
 * @s: an str as parameter.
 *
 * Return: 0 (Success)
*/

int _strlen(char *s)
{
	char s[] = "My first strlen!";

	printf("%d\n", strlen(str));

	return (0);
}
